<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asche.wetalk.mapper.LoggerMapper">

    <insert id="add" parameterType="com.asche.wetalk.entity.Logger">
        insert into logger ( args, ip, time, type, uri) values ( #{args}, #{ip}, #{time}, #{type}, #{uri})
    </insert>
</mapper>